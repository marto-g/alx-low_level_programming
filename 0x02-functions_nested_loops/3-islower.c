#include "main.h"

/**
 * _is lower - checks if character is lower case
 * @c; the character
 * Return; 1 if letter is lower case, 0 if not
 */
int _lower(int c)
{
	if c => 'a' && c <= 'z'
		return (1);
	else
		return (0);
